поверхностное копирование copy.copy() создает новый список с ссылками на старый,
при изменении нового списка исходный будет изменен,
глубокое копирование copy.deepcopy() создает новый список с копиями значений в старом списке,
при изменении - исходный список не изменяется
